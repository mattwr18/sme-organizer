<!DOCTYPE html>
<html>
<head>
  <title>SME Organizer</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
    <ul class="nav nav-tabs justify-content-end">
      <li class="nav-item">
        <%= active_link_to 'Home', root_path, active: :exclusive, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= active_link_to 'Sales', sales_path, active: :exclusive, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= active_link_to 'Purchases', purchases_path, active: :exclusive, class: "nav-link" %>
      </li>

      <% if user_signed_in? %>
        <li class="nav-item">
          <%= active_link_to "Logout", destroy_user_session_path, active: :exclusive, method: :delete, class: 'nav-link'  %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= active_link_to "Sign up", new_user_registration_path, active: :exclusive, class: 'nav-link'  %> |
        </li>
        <li class="nav-item">
          <%= active_link_to "Login", new_user_session_path, active: :exclusive, class: 'nav-link'  %>
        </li>
      <% end %>
    </ul>

  <% if notice %>
  <p class="alert alert-success"><%= notice %></p>
  <% end %>
  <% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
  <% end %>

  <%= yield %>
</body>
</html>
